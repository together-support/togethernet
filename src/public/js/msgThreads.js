export const replyThread = (event, textRecordNumber) => {
  // // let replyThread = `Reply Thread?`;
  // console.log(`START A THREAD? ?_?`);
  // // addSystemMsg(replyThread);

  // setTimeout(function () {
  //   let textBlob = document.getElementById(`textBlob-${textRecordNumber}`);
  //   if (textBlob) {
  //     textBlob.style.visibility = "visible";
  //   }
  // }, 0);

  // if (event.keyCode == 13) {
    // event.preventDefault();
  //   console.log("MAKE THREAD");
  // }
  // // allow user to reply thread
  // stopSendMsg = false;
}