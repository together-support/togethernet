import {historyMessage} from '../components/history.js';

// const record = "/record";
export const loadHistory = () => {
  // let response;
  // let data;

  // try {
  //   response = await fetch(url + record);  
  //   data = await response.json();
  // } catch (e) {
  //   alert('Could not load history')
  // }

  // archiveView = document.querySelector("#_publicMsg"); // archive view
  // data.forEach((message, idx) => {
  //   archiveView.appendChild(historyMessage(message, idx));
  // })
}