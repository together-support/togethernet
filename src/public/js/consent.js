import {addSystemMessage} from './systemMessage.js';

export const getConsent = (e) => {
  // addSystemMsg('Ask for Consent?');
  // if (e.keyCode == 13) {
  //   e.preventDefault();
  //   Object.values(peers).filter(peer => peer.send).forEach(peer => {
  //     peer.send('Can I get your consent to archive this message?');
  //     peer.send(name);
  //   });
  // }
  // stopSendMsg = true;
};